# Bug Report Triage RL Environment — Core Runtime
# Python 3.11+
# Used by Dockerfile / HF Space / inference.py / demo.py
# For RL training extras, see requirements-rl.txt

# Core
pydantic>=2.0
python-dotenv>=1.0
openenv-core>=0.1.0

# LLM + API
openai>=1.0
requests>=2.31
gradio>=4.0
fastapi>=0.100
uvicorn>=0.20

# Data + GitHub (for data pipeline)
PyGithub>=2.0

# Testing
pytest>=8.0
pytest-cov>=5.0
